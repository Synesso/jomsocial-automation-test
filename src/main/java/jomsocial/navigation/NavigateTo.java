package jomsocial.navigation;

import jomsocial.FrontPage;

public class NavigateTo {
    public static FrontPage frontPage() {
        // todo - where does the web driver come into this?
        return new FrontPage();
    }
}
